export default {
    privacyNotice(state) {
        return state.privacyNotice
    },
    getIndex(state) {
        return state.index
    },
    getConfigAnswers(state) {
        return state.configAnswers;
    }
}
