export default {
    privacyNotice(state) {
        return state.privacyNotice
    },
    getIndex(state) {
        return state.index
    },
    getConfigAnswer(state) {
        return state.configAnswers;
    }
}
